# Cloudflare Workers/Pages redirects
# HTTPS is enforced automatically by Cloudflare
# Only relative URLs are allowed for Workers

# Redirect www to non-www (if needed, configure at DNS level)
# For static sites, Cloudflare handles this automatically