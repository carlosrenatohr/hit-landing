# Public base URL of the tracking API (hit-ever2 worker). Exposed to the client (PUBLIC_ prefix).
# Local dev: copy to .env. Production: set in Cloudflare Pages → Settings → Environment variables.
# When a custom API domain is set up, switch this to https://api.hit-cargo.com.
PUBLIC_API_URL=https://hit-ever-scraper.honchkrow1995.workers.dev
