{
  "name": "hit-cargo-promo-tools",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tooling aislado para el promo video de la web (SPEC-P4-15): Playwright (captura del sitio real) + ffmpeg (render). No forma parte del bundle de producción.",
  "scripts": {
    "capture": "node capture.mjs",
    "render": "node render.mjs"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "playwright": "^1.49.1"
  }
}
