# HC-311 Re-run — 2026-08-23 (post PR #20/#22/#23 merge)

Target: local preview `wrangler pages dev dist --compatibility-date=2026-04-28` @ commit `6a9e1c6` (master, post-merge). Prod `hit-cargo.com` remains bot-challenged from this network (`cf-mitigated: challenge`), so per protocol §7 the CSP-true local preview is the evidence target.

## 1. Browser sweep — console / network / overflow

| Scope | Combos | Console errors | Failed requests | Horizontal overflow |
|---|---|---|---|---|
| `/` × 10 viewports (320→1920) | 10 | **0** | 0 | 0 |
| `/servicios`, `/servicios/aereo`, `/servicios/maritimo`, `/servicios/encomiendas`, `/track`, `/contacto`, `/precios` × (375/390/768/1280) | 28 | **0** | 0* | 0 |
| `/terminos`, `/privacidad` × (375/768/1440) | 6 | **0** | 0* | 0 |

\* The only non-2xx seen were wrangler's trailing-slash `308` redirects (normal static-pages behavior).

- **A-03 regression check: PASS.** The malformed-SVG console error previously hitting 12 home combos is gone (0 errors in all 44 combos).
- Sticky header, CTA visibility: no failures observed during sweeps.

## 2. Tracking events (dataLayer, live assertions)

| Event | Trigger tested | Result |
|---|---|---|
| `track_search` | Submit guía `910500` on `/track` | ✅ pushed (dl tail: gtm.js → track_search) |
| `whatsapp_click` | Click WhatsApp FAB on `/` | ✅ pushed |

Note: hero/CTA-section WhatsApp links (3 more `wa.me` anchors on home) are NOT instrumented — only the FAB pushes `whatsapp_click`. Relevant to G1 tag design.

## 3. Accessibility (axe-core 4.10.2, 390px, dark mode)

| Violation | Impact | Pages affected | Detail |
|---|---|---|---|
| `color-contrast` | serious | home(4), contacto(4), precios(5), servicios(2), aereo(2) | White text on `bg-primary` (#FF7A00) = **2.61:1** (needs 4.5:1 / 3:1 large). Affects every primary CTA button + one #fcee… text-on-orange. Brand-level tension: orange bg needs navy (#14213D ≈ 6:1 ✅) or darker text. |
| `link-name` | serious | ALL pages (3 nodes) | Footer social icons (Facebook/Instagram/TikTok, `Footer.astro`) have no accessible text → add `aria-label`. |
| `region` | moderate | ALL pages (1 node) | Content outside landmarks (minor, common). |
| `heading-order` | moderate | /track, /servicios/aereo | Skipped heading level. |
| duplicate/top-level `<main>` landmarks | moderate | /track | Duplicate `main` landmark on tracking page. |

## 4. Screenshots (this folder)

- `rerun-home-390.png`, `rerun-home-1440.png`, `rerun-track-result-390.png`

## 5. Verdict

- Checklist **B-05 (no console errors): now ✅** with fresh evidence.
- Checklist **C-05 (contrast AA): FAIL confirmed by axe** — primary CTA contrast is systematic, not incidental.
- New P2 findings for backlog: social icon `aria-label`s; navy-text-on-orange option for CTAs; `/track` heading order + duplicate main.
- No P0/P1 regressions after merging audit batches 1–3.

Evidence run: Playwright MCP (chromium), 2026-08-23, against dist built from master `6a9e1c6`.
